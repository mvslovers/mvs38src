# Utility listings snapshotted from failing build jobs. Capped by bldrun.py
# to 8,003 lines, but a run started before that cap can still leave a 77 MB
# ASMPRINT here. Keep the small diagnostic ones; never let a bulk listing in.
*.ASMPRINT.txt
*.UPDPRINT.txt
